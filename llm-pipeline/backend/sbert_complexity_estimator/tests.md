# Run tests

```bash
curl -X POST http://localhost:8007/estimate-all \
  -H "Content-Type: application/json" \
  -d '{"description": "Build a Flask service to expose machine learning model predictions."}'
```